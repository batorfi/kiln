// Fixture for pi-flag.test.ts — tests pass, but NONE is named `PI-LIVE:` (the situation of `--pi` pointed at the wrong directory).
import { test } from "node:test";
test("an ordinary passing test", () => {});
